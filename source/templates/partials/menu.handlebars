<nav>
    <div class="fst Ta-r">
        <ul>
            {{#menu}}
            <li class="D-ib Ta-c nav-li">
                <a class="nav-li-a Fz-3 Td-0 D-b W-100 H-100" href="{{url}}" data-index={{@index}}>
                    {{name}}
                </a>
                {{#if children}}
                <div class="snd Pos-a W-100 L-0 Z-3 Ov-h Ta-l">
                    <div class="M-0a wrap">
                        <h2 class="Font-2 C-wht Fw-1 Fz-8 Mb-1 Ls-5">{{name}}</h2>
                        <div class="desc D-ib Va-t Ws-n" style="width:400px;">
                            {{#if desc}}
                            <p class="M-0 C-grey2">{{desc}}</p>
                            {{/if}}
                        </div>
                        <ul class="snd-ul D-ib">
                            {{#children}}
                            <li>
                                <a class="Td-0" href={{url}}>{{name}}</a>
                                {{#if children}}
                                <div class="trd">
                                    <ul>
                                        {{#children}}
                                        <li>
                                            <a class="Td-0" href="{{url}}">{{name}}</a>
                                        </li>
                                        {{/children}}
                                    </ul>
                                </div>
                                {{/if}}
                            </li>
                            {{/children}}
                        </ul>
                    </div>
                </div>
                {{/if}}
            </li>
            {{/menu}}
        </ul>
    </div>
</nav>